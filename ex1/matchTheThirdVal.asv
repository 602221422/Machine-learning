function [X,Y,Z] = matchTheThreeVal(data)
% Compute the third value for surf(X,Y,Z)
%  X = data(:,1)    X是data的第一列数据
%  Y = data(:,2)    Y是data的第二列数据
%  


% ====================== MY CODE HERE ======================
% Instructions: Compute the cost of a particular choice of theta
%               You should set J to the cost.

X = data(:,1);
Y = data(:,2);
meshgrid(X,Y);


% =========================================================================

end
