function W = outlayer(index, num)
%生成一个向量，仅在index位 为1 其余位置为0  用于神经网络outlayer 中的y

W = zeros(num, 1); %初始化W为num 行1列的全零向量

% ====================== MY CODE HERE ======================
%

W(index)=1;

% =========================================================================

end
